# pet925 プロジェクト・サマリー

## 🏆 到達レベル
**実戦的な開発現場で通用する「ミドルエンド級エンジニア」**
単にコードを書くだけでなく、運用の効率化、データの安全性、自動化を統合して設計・構築できるレベルに到達しています。

## 🛠 習得・活用した技術
1. **データ駆動型アーキテクチャ (Data-Driven)**
   - 人間が編集する「マスターデータ(.ods)」と、プログラム用の「中間データ(.csv)」、Web用の「配信データ(.js)」を分離するプロの設計思想。
2. **静的サイト生成 (SSG) と自動化**
   - Node.js を使ったビルドパイプラインの構築。
   - `fs.watch` によるファイル変更の自動検知（監視モード）。
3. **データ品質管理 (QA)**
   - 正規表現（Regex）を駆使した高度な CSV パース処理。
   - 許可タグリストに基づく自動バリデーション（入力ミス検知）。
   - **Jest** による自動ユニットテストの導入。（`convert.test.js`）
4. **モダン・フロントエンド**
   - Vanilla JS によるデータ駆動の UI 生成。
   - **CLS (Cumulative Layout Shift) 対策**: 複数選択ボタンの幅を固定し、チェックマークを絶対配置することで操作時のガタつきを解消。
   - CSS Grid/Flexbox や Backdrop-filter を使ったカタログ風デザイン。
   - **表示パフォーマンス最適化**: `loading="lazy"` や `decoding="async"` の導入。
   - **堅牢なプレースホルダー**: Data URI (SVG) を使った、外部リクエスト不要の「No Image」表示ロジック。

## 🛠 トラブルシューティングと学びの記録

### 1. ディレクトリ構造の最適化とパスの管理
- **経過**: `public/` フォルダを廃止し、`index.html` と `data.js` をルートへ移動してデプロイを簡略化。
- **学び**: Node.js の `__dirname` と `path.join` を使った相対パス指定の重要性。フォルダ構成変更時にスクリプト内の出力先パスを同期させないと `ENOENT` エラーが発生することを実体験として理解。

### 2. 関数設計とスコープの理解 (`ReferenceError`)
- **経過**: `useHeaderMap is not defined` エラーを、関数の引数設計を見直すことで解決。
- **学び**: 関数に引数を追加した際、呼び出し側と定義側の整合性を保つ重要性。また、デフォルト引数 `(content, useHeaderMap = true)` を活用することで、既存コードを壊さずに機能を拡張する手法を習得。

### 3. CI環境 (GitHub Actions) への対応
- **経過**: GitHub Actions 上でエラーが発生した際、何が原因か GitHub の画面上ですぐに分かるように「日本語のヒント」と「Workflow Commands」を導入。
- **学び**: 実行環境（`process.env.GITHUB_ACTIONS`）を検知して挙動を変える手法や、エラー時に終了コード `1` を返して安全にパイプラインを止める「壊さない開発」の考え方を習得。

### 4. 汎用的な関数によるコードの共通化 (DRY原則)
- **経過**: `tags.csv` や `brands.csv` の読み込み処理を `loadSettingsCSV` 関数に統合。
- **学び**: 重複する処理を一つにまとめることで、バグを減らしメンテナンス性を高める「プロの設計」を実践。

### 5. ブラウザのキャッシュ管理
- **経過**: コード修正が画面に反映されない現象への対処。
- **学び**: Web開発特有の挙動であるブラウザキャッシュへの理解。開発中の **Ctrl + F5**（強力な再読み込み）の習慣化。

### 6. Google Analytics (GA4) 導入とデバッグ
- **経過**: GA4の計測がローカルで動かず、本番環境でも検出されない問題に直面。
- **学び**:
    - `index.html` でのURL同期 (`updateURL`) により、SPA（シングルページアプリケーション）形式のサイトでもGA4の `page_view` イベントを正確に計測する手法。
    - `?q=`, `?animal=`, `?cond=` などのクエリパラメータをGA4のサイト内検索レポートで活用するための設定。
    - `event_category`, `event_label` をカスタムディメンションとして登録し、アフィリエイトクリックやフィルタ選択を詳細に分析する手法。
    - ローカル開発環境 (`file:///`) でのGA4計測の課題 (`cookie_domain: 'none'`) と `Live Server` の活用。
    - `debug_mode: true` を用いた `DebugView` でのリアルタイム検証の重要性。
    - `favicon.ico` の404エラーは計測に影響しないことの理解。
    - ブラウザの広告ブロックやトラッキング防止機能がGA4計測を阻害するケースとその対処法。

### 7. Git運用とデプロイの効率化
- **経過**: `data.js` の更新忘れやGit競合により、デプロイが停止する問題が発生。
- **学び**:
    - `git add .` の重要性（特に `convert.js` が生成する `data.js` の追跡）。
    - `npm run deploy` コマンドによる `add`, `commit`, `push` の一括実行で、デプロイプロセスを自動化・簡素化。
    - Git競合 (`<<<<<<< HEAD`) 発生時の `git merge --abort` と `git push -f` による強制同期の習得。
    - VS Codeの自動貼り付け（Apply）が失敗する原因（コードのズレ）と手動対処法。

### 8. UI/UX の微細なこだわりとアクセシビリティ
- **経過**: 「こだわり」タグを選択した際にボタンが動く問題を修正。また、ラベルを「こだわり・お悩み」に変更。
- **修正**: フィルターカテゴリおよび商品カード内タグの表示順を「種類 (Animal) → 年齢 (Age)」に完全固定。カテゴリ名の大文字小文字の揺れも吸収。
- **UI整理**: 商品カード内の基本タグ（種類・年齢）を非表示化。冗長な情報を削ぎ落とし、「こだわり・お悩み」タグのみを際立たせるミニマルな設計へ。
- **注釈の削除**: グレインフリー商品に表示されていた「※この商品はグレインフリーです」という冗長な注釈テキストを削除。
- **テキスト表示の最適化**: 商品名のフォントサイズを微調整（1.15rem -> 1.05rem）し、最大表示行数を3行に拡張。長名称の商品でも情報が途切れにくい設計に改善。
- **レイアウトの最適化**: コンテンツ量に関わらずボタン位置やテキスト領域が揃う「垂直カード・グリッドレイアウト」を最終採用。ガタつきのない安定した視認性を確保。
- **追加**: サイトコンセプトを説明する導入文をヘッダーに追加。
- **微調整**: 導入文を「ー愛犬・愛猫にぴったりのフードを探せるお手伝いをー」へ変更。より親しみやすく、かつ極限までシンプルに整理。
- **学び**: 
    - ユーザーが操作した瞬間にレイアウトが崩れる不快感を取り除く重要性。
    - 文言ひとつでユーザーが「自分に向けられたサイトだ」と感じる心理的効果（こだわり -> こだわり・お悩み）。
    - 初見のユーザーに対して、サイトの価値（何ができるか）を言語化して提示することの信頼性向上効果。

### 9. 画像の最適化と信頼性
- **経過**: 画像未設定時のプレースホルダーが表示されない問題を、Data URI SVG を使って解決。
- **学び**: 外部リソース（Unsplash等）に依存しすぎず、自律的に動くコードを書く重要性。

### 10. データの「名寄せ（正規化）」の自動化
- **経過**: `brands.csv` に登録がある場合、入力が多少揺れていても正式名称に自動変換するロジックを `convert.js` に追加。
- **進化**: ブランド表示を「入力されたアルファベットのまま」維持しつつ、裏側で日本語検索を可能にする `brand_id` 方式へ移行。
- **学び**: サイトの「高級感（アルファベット表示）」と「利便性（日本語検索）」を両立させるためのデータ設計手法。

### 11. ユニットテストによる安全性の担保
- **経過**: CSVの引用符閉じ忘れ、列数不一致、価格の数値チェックなどを `convert.test.js` で網羅。
- **学び**: 「手動でマージしたコードが正しいか」を自分やAIに聞くのではなく、テストプログラムに証明させるというプロの習慣。

##  実施内容の記録

### 1. データ管理の革新
- **統合マスターファイル (Unified Master)**: `products`, `tags`, `brands` を一つの `.ods` 内でシート別に管理。マクロによる一括CSV出力により、運用のヒューマンエラーを完全に排除。
- **アフィリエイト・価格連携**: A8.net、Amazon、楽天、Yahoo!の価格比較列を追加し、収益化の基盤を作成。

### 2. 検索・フィルタリングの高度化（New）
- **「製法・原材料」グループの確立**: 「穀物不使用」や「ラム」「魚」を独立させ、ユーザーが本当に求める条件をクイックに選択可能に。
- **エイリアス機能（自動タグ付け）**: サーモンやマグロといった具体的な魚種から、自動で「魚」フィルタを有効にするスマートロジックを搭載。
- **日本語検索の正規化と検索精度の最適化**: ひらがな・カタカナの区別を無くし、検索対象を「商品名」と「ブランド名」に絞り込むことで、ノイズの少ない的確な検索体験を実現。
- **複数選択（マルチセレクト）と絞り込みの深化**: 「製法・原材料」「こだわり」グループで複数のタグを同時に選択可能。チェックを入れるほど条件が限定される**AND検索ロジック**を採用し、ユーザーが求める「究極の一皿」に辿り着きやすい設計に改善。
- **タグ構成の最適化**: 運用に合わせて「ドライ」タグを廃止するなど、ユーザーの選択肢を常に最適化。

### 3. システムのインテグレーション
- **変換スクリプト (`convert.js`)**: CSVを高速に JS へ変換する心臓部を開発。
- **監視モードとデバウンス処理**: 保存するだけで即座に反映される環境に加え、連続保存による多重実行を防ぐ待機ロジック（Debounce）を搭載。
- **ブランドの自動名寄せ**: `nutro` と入力されても `ニュートロ` と出力し、検索用IDとして `brand_id` を保持するインテリジェントな変換。
- **タグマスターの外部化 (`tags.csv`)**: フィルターのカテゴリや表示名を外部ファイルで管理。プログラムを修正することなく、スプレッドシートの操作のみでサイト構成の変更が可能に。
- **ブランドエイリアスの外部化 (`brands.csv`)**: 英語ブランド名と日本語検索ワードの紐付けを外部化し、検索の柔軟性を向上。
- **タグの一元管理**: 1箇所の修正で「チェック機能」と「サイトのボタン」が連動する仕組み。
- **自動リンク生成**: 個別URLがない場合も、商品名で各モールの検索結果ページへ自動誘導。
- **エラー画面の親切化**: 検索結果が0件の際のおしゃれなメッセージ表示。
### 5. Google Analytics (GA4) 連携

### 4. ユーザー体験 (UX) の向上
- **商品名・ブランド検索**: ユーザーが探している特定の商品やメーカーを素早く見つけられるよう、検索窓の役割を最適化。
- **サイトコンセプトの明文化**: ヘッダーに導入文を追加し、サイトの目的を明確に提示。
- **タイル・レイアウト**: フィルタパネルに画面幅に合わせて折り返すタイル形式を採用し、全選択肢を俯瞰しやすく改善。
- **自動リンク生成**: 個別URLがない場合も、商品名で各モールの検索結果ページへ自動誘導。
- **ショップ横断検索ナビ**: 3モールのURLが未入力でも、ブランド名＋商品名から最適な検索結果ページを動的に生成するロジックを搭載。
- **画像フォールバック**: 画像未設定やエラー時にデフォルト画像を表示する、リンク切れを恐れない堅牢なフロントエンド。
- **エラー画面の親切化**: 検索結果が0件の際のおしゃれなメッセージ表示。

### 5. Google Analytics (GA4) 連携
- **URLパラメータと連動したサイト内検索の自動計測**: ユーザーの検索行動をGA4で詳細に分析可能に。
- **アフィリエイトリンクおよびフィルタ選択のイベント計測**: Amazon, 楽天, Yahoo!, A8へのクリックや、各フィルタの利用状況をイベントとして収集。
- **検索結果0件時のイベント送信**: ユーザーが求める情報が見つからなかったキーワードをGA4で把握し、コンテンツ拡充のヒントに。

### 6. 開発ワークフローの自動化
- **`npm run deploy` コマンドによるデータ更新からGitHub Pagesへの公開までの一括自動化**: 運用負荷を大幅に軽減。
- **`convert.js` 実行時のデータ集計レポート**: 変換時にブランド・タグの分布や未設定商品をコンソールに表示し、データ品質管理を強化。

### 5. 運用のプロフェッショナル化
- **詳細なマニュアル (`MANUAL.md`)**: タグリスト、運用ルーチン、トラブル対処法を完備。
- **プロジェクトドキュメント (`README.md`)**: プロジェクトの目的やAIへの指示書を整理。
- **テストコード (`convert.test.js`)**: ロジックが壊れていないか機械が証明する仕組み。

---

## 🤖 AI への指示用テンプレート (最新版)
Gemini に相談する際は、以下の情報をコンテキストとして渡してください。

### プロジェクト背景
15列構成のCSVから `data.js` を生成し、`index.html` で動的表示するペットフード比較サイト。

### 現在の構成
- `pet925_master.ods` (4シート構成: products, tags, brands, rules) が唯一の真実。
- `convert.js` が監視・変換・バリデーションを担当。
- `index.html` がタグ生成・価格比較・描画を担当。

### ルール
- タグは `tags.csv` に定義されている `key` のみを使用する。
- 列構成：`name,brand,tags,desc,size,img,amz,rak,yah,a8,label,promo,amz_p,rak_p,yah_p`

---
**最終更新日**: 2024年5月10日（UI安定化、画像最適化、ブランド自動変換の反映）
**作者**: kyuni
**職業的立ち位置**: フルスタック・個人開発者（Indie Hacker）